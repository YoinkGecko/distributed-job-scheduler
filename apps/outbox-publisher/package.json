{
  "name": "@scheduler/outbox-publisher",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@scheduler/types": "workspace:*",
    "@scheduler/redis": "workspace:*",
    "@scheduler/database": "workspace:*"
  }
}