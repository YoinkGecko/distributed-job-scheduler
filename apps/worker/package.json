{
  "name": "@scheduler/worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/worker.ts",
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@scheduler/redis": "workspace:*",
    "@scheduler/types": "workspace:*",
    "dotenv": "^17.4.2",
    "pg": "^8.22.0"
  },
  "devDependencies": {
    "@types/pg": "^8.20.0"
  }
}