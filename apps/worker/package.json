{
  "name": "@scheduler/worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/worker.ts",
    "rw": "tsx watch src/recovery.worker.ts",
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@scheduler/redis": "workspace:*",
    "@scheduler/types": "workspace:*",
    "@scheduler/database": "workspace:*",
    "dotenv": "^17.4.2"
  },
  "devDependencies": {
  }
}